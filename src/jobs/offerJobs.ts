// Stub: offer system not yet on main branch
// This prevents import errors in security-fixed controllers
export async function expireOffersForSoldItem(_listingId: string): Promise<number> {
  // No-op until offer system is merged
  return 0;
}
